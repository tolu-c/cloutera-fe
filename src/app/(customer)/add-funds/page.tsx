import { AddFunds } from "@/components/add-funds";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Cloutera | Add Funds",
};

const AddFundsPage = () => {
  return <AddFunds />;
};

export default AddFundsPage;
